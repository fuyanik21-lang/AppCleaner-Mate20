from pathlib import Path
import sys
root = Path(sys.argv[1]) if len(sys.argv) > 1 else Path("AppCleaner-Mate20-TAM-PROJE")
files = {
    'app/src/main/java/com/personal/appcleaner/ui/AppCleanerApp.kt': 'package com.personal.appcleaner.ui\n\nimport androidx.compose.foundation.layout.Box\nimport androidx.compose.foundation.layout.padding\nimport androidx.compose.material.icons.Icons\nimport androidx.compose.material.icons.filled.CleaningServices\nimport androidx.compose.material.icons.filled.History\nimport androidx.compose.material.icons.filled.Folder\nimport androidx.compose.material.icons.filled.Security\nimport androidx.compose.material.icons.filled.Settings\nimport androidx.compose.material3.AlertDialog\nimport androidx.compose.material3.Icon\nimport androidx.compose.material3.NavigationBar\nimport androidx.compose.material3.NavigationBarItem\nimport androidx.compose.material3.Scaffold\nimport androidx.compose.material3.Text\nimport androidx.compose.material3.TextButton\nimport androidx.compose.runtime.Composable\nimport androidx.compose.runtime.getValue\nimport androidx.compose.ui.Modifier\nimport androidx.lifecycle.compose.collectAsStateWithLifecycle\nimport androidx.navigation.compose.NavHost\nimport androidx.navigation.compose.composable\nimport androidx.navigation.compose.currentBackStackEntryAsState\nimport androidx.navigation.compose.rememberNavController\nimport com.personal.appcleaner.MainViewModel\nimport com.personal.appcleaner.ui.screens.CleanScreen\nimport com.personal.appcleaner.ui.screens.ExcludedScreen\nimport com.personal.appcleaner.ui.screens.HistoryScreen\nimport com.personal.appcleaner.ui.screens.SettingsScreen\nimport com.personal.appcleaner.ui.screens.StorageCleanerScreen\n\nprivate data class Tab(val route: String, val label: String, val icon: androidx.compose.ui.graphics.vector.ImageVector)\n\n@Composable\nfun AppCleanerApp(vm: MainViewModel) {\n    val state by vm.ui.collectAsStateWithLifecycle()\n    val settings by vm.settings.collectAsStateWithLifecycle()\n    val history by vm.history.collectAsStateWithLifecycle()\n    val storage by vm.storage.collectAsStateWithLifecycle()\n    val nav = rememberNavController()\n    val backStack by nav.currentBackStackEntryAsState()\n    val tabs = listOf(\n        Tab("clean", "Uygulama", Icons.Default.CleaningServices),\n        Tab("storage", "Dosyalar", Icons.Default.Folder),\n        Tab("excluded", "Hariç", Icons.Default.Security),\n        Tab("history", "Geçmiş", Icons.Default.History),\n        Tab("settings", "Ayarlar", Icons.Default.Settings)\n    )\n\n    Scaffold(bottomBar = {\n        NavigationBar {\n            tabs.forEach { tab ->\n                NavigationBarItem(\n                    selected = backStack?.destination?.route == tab.route,\n                    onClick = { nav.navigate(tab.route) { launchSingleTop = true; restoreState = true; popUpTo("clean") { saveState = true } } },\n                    icon = { Icon(tab.icon, tab.label) },\n                    label = { Text(tab.label) }\n                )\n            }\n        }\n    }) { padding ->\n        Box(Modifier.padding(padding)) {\n            NavHost(navController = nav, startDestination = "clean") {\n                composable("clean") { CleanScreen(state, vm) }\n                composable("storage") { StorageCleanerScreen(storage, settings, vm) }\n                composable("excluded") { ExcludedScreen(state, vm) }\n                composable("history") { HistoryScreen(history, vm) }\n                composable("settings") { SettingsScreen(state, settings, vm) }\n            }\n        }\n    }\n\n    state.message?.let { message ->\n        AlertDialog(\n            onDismissRequest = vm::clearMessage,\n            text = { Text(message) },\n            confirmButton = { TextButton(onClick = vm::clearMessage) { Text("Tamam") } }\n        )\n    }\n}\n',
    'app/build.gradle.kts': 'plugins {\n    id("com.android.application")\n    id("org.jetbrains.kotlin.android")\n    id("org.jetbrains.kotlin.plugin.compose")\n}\n\nandroid {\n    namespace = "com.personal.appcleaner"\n    compileSdk = 36\n\n    defaultConfig {\n        applicationId = "com.personal.appcleaner"\n        minSdk = 28\n        targetSdk = 36\n        versionCode = 2\n        versionName = "1.1.0"\n\n        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"\n    }\n\n    buildFeatures {\n        compose = true\n        aidl = true\n        buildConfig = true\n    }\n\n    compileOptions {\n        sourceCompatibility = JavaVersion.VERSION_17\n        targetCompatibility = JavaVersion.VERSION_17\n    }\n\n    kotlinOptions {\n        jvmTarget = "17"\n    }\n\n    buildTypes {\n        release {\n            signingConfig = signingConfigs.getByName("debug")\n            isMinifyEnabled = false\n            proguardFiles(\n                getDefaultProguardFile("proguard-android-optimize.txt"),\n                "proguard-rules.pro"\n            )\n        }\n    }\n\n    packaging {\n        resources.excludes += setOf("/META-INF/{AL2.0,LGPL2.1}")\n    }\n}\n\ndependencies {\n    val composeBom = platform("androidx.compose:compose-bom:2026.04.01")\n    implementation(composeBom)\n    androidTestImplementation(composeBom)\n\n    implementation("androidx.core:core-ktx:1.17.0")\n    implementation("androidx.activity:activity-compose:1.10.1")\n    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.9.1")\n    implementation("androidx.lifecycle:lifecycle-runtime-compose:2.9.1")\n    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.9.1")\n    implementation("androidx.navigation:navigation-compose:2.9.0")\n    implementation("androidx.datastore:datastore-preferences:1.1.7")\n    implementation("androidx.work:work-runtime-ktx:2.11.2")\n\n    implementation("androidx.compose.ui:ui")\n    implementation("androidx.compose.ui:ui-tooling-preview")\n    implementation("androidx.compose.foundation:foundation")\n    implementation("androidx.compose.material3:material3")\n    implementation("androidx.compose.material:material-icons-extended")\n    debugImplementation("androidx.compose.ui:ui-tooling")\n\n    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.10.2")\n    implementation("dev.rikka.shizuku:api:13.1.5")\n    implementation("dev.rikka.shizuku:provider:13.1.5")\n\n    testImplementation("junit:junit:4.13.2")\n    testImplementation("org.jetbrains.kotlinx:kotlinx-coroutines-test:1.10.2")\n}\n',
}
for rel, content in files.items():
    p = root / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content)
readme = root / "README.md"
s = readme.read_text()
if "## Dosya Temizliği (v1.1)" not in s:
    readme.write_text(s + "\n\n## Dosya Temizliği (v1.1)\n\nDosya Temizliği ekranı Android Storage Access Framework (SAF) kullanır. Uygulama geniş depolama izni istemez. Kullanıcı taranacak klasörü kendisi seçer. Tarama; geçici dosya, log, yedek/artık, boş dosya, eski APK ve inceleme amaçlı büyük dosya adaylarını gösterir. Fotoğraf, video, belge ve Android uygulama özel verileri otomatik silinmez. Eski APK ve büyük dosya adayları varsayılan olarak seçilmez. Silme öncesinde açık onay istenir.\n")
print("Storage cleaner v1.1 part2b applied")
